Show countVowels(text: text) -> num {
    Make vowels: list<text> = ["a", "e", "i", "o", "u"];
    Make count: num = 5;
    Keep char in text {
        When char in vowels {
            count = count plus 1;
        }
    }
    give count;
}
